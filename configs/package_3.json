{
  "name": "claude-code-deployment",
  "version": "1.0.0",
  "description": "Claude Code Remote & UI Combined Deployment",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build required for static HTML'",
    "start": "echo 'Static site ready'"
  }
}